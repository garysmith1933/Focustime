import React, {useState, useEffect} from 'react';
import { View, Text, StyleSheet, Platform } from 'react-native';

export const Planner = () => {
    return (
        <View style={{flex: 1, padding: 32}}> 
            <Text style={{fontSize: 32}}> This is the Planning Page! </Text>
        </View>
    )
}